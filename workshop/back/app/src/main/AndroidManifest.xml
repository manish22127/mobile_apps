<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.back4app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Back4app">
        <activity
            android:name=".Second"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data android:value="@string/back4app_server_url" android:name="com.parse.SERVER_URL"></meta-data>
        <meta-data android:value="@string/back4app_client_key" android:name="com.parse.CLIENT_KEY"></meta-data>
        <meta-data android:value="@string/back4app_app_id" android:name="com.parse.APP_ID"></meta-data>
    </application>




</manifest>